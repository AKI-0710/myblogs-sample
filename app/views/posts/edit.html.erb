<div class="contents row">
  <%= form_for @post, :url => {:action => 'update'} do |f| %>
    <p>記事を投稿する</p>
    <div>
      <%= f.text_field :article, placeholder: "#{@post[:article]}" %>
    </div>
    <div>
      <%= f.submit %>
    </div>
  <% end %>
</div>